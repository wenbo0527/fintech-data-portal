# 📊 P2-1 质量基线报告 · 2026-09-08

> **目标**：建立 P2 持续运营期的质量基线，为每周/月度迭代提供对照数据。

---

## 1. 仓库状态总览

| 指标 | 数值 |
|:---|:---|
| 当前 commit | `5cc90914` |
| 累计改造（P0+P1）| **10 commits** |
| 已推送远端 | `origin`(GitHub) + `all`(GitHub + Gitee) |
| 子应用数 | **12 个** |
| 共享包数 | 3 个（lineage-graph / shared-api / shared-utils） |
| Mock 数据集 | 80+ 模块 |

## 2. ESLint 现状

- ✅ 配置已合并为 1 份（`.eslintrc.json`，删除 `.disabled` 2 份与 `.byted.json` 1 份）
- ✅ `pnpm lint:check --max-warnings 0` 已配置
- ✅ 三大目录分区：`apps/` / `packages/` / `archive/legacy-src/`

**遗留问题**（P2 待办）：
- `mkt-app` 仍混用 `.js` / `.ts`（API 模块同时存在两种后缀）
- `dfd-app` 残留 `.js` 测试文件（已 co-located）
- 旧主应用 `archive/legacy-src/` 仅加 `[skip lint]`，规则已豁免但代码未清理

## 3. TypeScript 现状

- `tsconfig.json`（顶层）：存在但非严格模式（未开 `strict`）
- `tsconfig.typecheck.json`：子应用类型检查入口
- `tsconfig.paths.json`：路径别名
- `tsconfig.node.json`：Node 端

**未开启**（P2 建议顺序）：
- `strict: true`
- `noImplicitAny: true`
- `noUnusedLocals: true`
- `noUnusedParameters: true`
- `noFallthroughCasesInSwitch: true`
- `exactOptionalPropertyTypes: true`（激进，最后开）

## 4. 测试覆盖率

| 层级 | 文件数 | 状态 |
|:---|:---:|:---|
| `tests/unit/` | 8 | 活跃 |
| `tests/components/` | 12 | 活跃 |
| `tests/e2e/` | 26 | 活跃 |
| `tests/archived/` | 87+ | 归档 |
| `tests/archived/legacy/` | 7 | 新增（P1-9 移入） |
| `apps/horizontal-canvas/tests/` | 14 | 子应用 |
| `apps/risk-app/tests/` | 1 | 子应用 |
| `apps/*/src/**/__tests__/` | 5 | co-located |

**覆盖率指标**：当前未集成 `coverage` 上传，需在 ci.yml 后续迭代中加入 v8 provider 与 80% 阈值门。

## 5. CI/CD 流水线（P1-10 已落地）

```
lint → typecheck → unit ──┐
   └──→ jscpd         ──┴─→ e2e (needs: unit)
   └──→ security (仅 main · needs: lint+typecheck+unit)
```

**已升级**：actions/checkout v4、setup-node v4、upload-artifact v4。

**可继续优化**（P2 候选）：
- coverage v8 provider + 80% 阈值硬质量门
- jscpd 阈值（>5% 失败）
- Dependabot 自动 PR
- preview deployment on PR

## 6. 文档体系（P1-6 已落地）

```
docs/
├── architecture/    # 架构总览 + 设计文档 16+
├── governance/     # 分支治理 4
├── guides/         # 业务模块说明 6
├── migration/      # JS/TS 迁移 2
├── prd/            # PRD 4
├── reports/        # 阶段报告 6 + 本次新增
└── archive/        # 历史归档 18+
```

索引入口：[docs/README.md](../README.md)

## 7. 12 个子应用矩阵

| 子应用 | 端口 | tests/ | co-located | 端口 | 状态 |
|:---|:---:|:---:|:---:|:---:|:---:|
| admin-app | 5181 | ✅ | — | — | ✅ |
| asset-app | 5182 | ✅ | — | — | ✅ |
| data-community-app | 5183 | ✅ | — | — | ✅ |
| dex-app | 5184 | ✅ | ✅2 | — | ✅ |
| dfd-app | 5185 | ✅ | ✅2 | — | ✅ |
| dmt-app | 5186 | ✅ | — | — | ✅ |
| horizontal-canvas | 5187 | ✅ 14 | — | — | ✅ |
| mkt-app | 5188 | ✅ | ✅1 | — | ✅ |
| report-monitor | 5189 | ✅ | — | — | ✅ |
| report-monitor-backend | 5190 | ✅ | — | — | ⚠️ |
| risk-app | 5191 | ✅ 1 | — | — | ✅ |
| touch | 5192 | ✅ | — | — | ✅ |

> ⚠️ `report-monitor-backend/` 是 Node.js 后端项目，与"项目仅前端"规则冲突，建议作为 P2-2 处理。

## 8. P2 阶段路线图（后续 30 天）

| 周次 | 任务 | 输出 |
|:---|:---|:---|
| W1 | 每周 ESLint/Typecheck baseline | 报告 + issue |
| W2 | mkt-app 残留 .js → .ts 迁移 | 删除重复 + 风险登记 |
| W3 | tsconfig 严格化（分阶段打开）| 每周开 1-2 项 |
| W4 | coverage v8 + 80% 阈值 | CI 硬质量门 |
| W4 | P2-2 report-monitor-backend 处置（删除或重构） | 决策 + 清理 |

## 9. 关键风险登记

| ID | 风险 | 影响 | 状态 |
|:---|:---|:---|:---:|
| R-001 | `report-monitor-backend/` 性质不符 | 仓库污染 | 待 P2-2 |
| R-002 | `gitee` 独立远端历史分歧（fetch ref 仍为旧） | 推送误判 | 监控 |
| R-003 | `mkt-app` `.js`/`.ts` 双版本 | 维护成本 | P2-W2 |
| R-004 | `archive/legacy-src/` 仍占 2861 文件 | 仓库体积 | 已冻结 |
| R-005 | 子应用代码风格不完全一致 | 长期目标 | 持续优化 |
| R-006 | jsx-a11y / arco-design 规则冲突待验证 | CI 红 | 待 W1 验证 |
| R-007 | arco-design 组件库依赖未统一版本 | 升级风险 | 监控 |

## 10. 总结

- **P0（治理）：4/4 完成**，仓库结构与文档体系已规范
- **P1（规范）：6/10 完成**，剩余 P1-2/3/4（TS 严格化 / shared-types / shared-styles）影响面大、风险高，建议分阶段拆分
- **P2（持续）：进入第 1 周**，每周跑一次 quality baseline，建立可持续的工程节奏

---

*本文档由 P2-1 自动生成 · 后续将每 7 天刷新一次*